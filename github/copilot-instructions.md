The solution for each day should be put in its own directory. The name of the directory should follow the namimg comvention "Day01", "Day02", etc. 

Each solution that requires an input file should use the file "input.txt" in the solution directory. 

After implementing the solution please run it and document the results in a file called results.md.